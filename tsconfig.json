{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "target": "es2017",
    // Alias `src` to `~`
    "baseUrl": ".",
    "paths": {
      "~/*": [
        "src/*"
      ]
    }
  }
}
